"use client"

import { getUserApi } from "@/services/login"


export default function DashBoard() {

    // const { data: getUser } = getUserApi()
    return (
        <>
            {/* <p>{getUser?.data?.firstName}</p> */}
        </>
    )
}